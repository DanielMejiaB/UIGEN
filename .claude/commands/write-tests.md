Write comprehensive tests for: $ARGUMENTS

Testing conventions:

* Use vitests with React Testing Library.
* place tests in `__tests__` directory in the same folder as the code being tested.
* name test files [filename].test.ts(x)
* use @/ prefix for imports

Coverage:

* Test happy paths
* Test edge cases
* Test error states